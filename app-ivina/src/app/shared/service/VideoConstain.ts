export enum WebRTCEvent{
    offer = "offer",
    answer = "answer",
    candidate = "candidate",
    // disconnected = "disconnected"
}

export enum CallEvent {
    call = 'call',
    reject = 'reject',
    receive = 'receive'
}